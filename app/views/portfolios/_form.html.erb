<%= form_with(model: portfolio) do |form| %>
  <% if portfolio.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(portfolio.errors.count, "error") %> prohibited this portfolio from being saved:</h2>

      <ul>
        <% portfolio.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :welcome_message, style: "display: block" %>
    <%= form.text_field :welcome_message %>
  </div>

  <div>
    <%= form.label :about_me_text, style: "display: block" %>
    <%= form.text_area :about_me_text %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
