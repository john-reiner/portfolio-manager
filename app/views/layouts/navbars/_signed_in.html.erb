<nav class="navbar navbar-expand-sm navbar-dark bg-dark">
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent" data-bs-toggle="popover" data-bs-placement="bottom" data-bs-content="Bottom popover">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <%= link_to  @portfolio.email, portfolio_path(@portfolio), class: "nav-link active" %>
                </li>
            </ul>
            <% if flash[:notice] %>
                <div id="passwordHelpInline" class="text-info" style="margin-right: 1em;">
                    <%= flash[:notice] %>
                </div>
            <% end %>
            
            <li class="nav-item d-flex">
                <%= link_to  "Sign Out", logout_path, class: "nav-link active" %>
            </li>
        
        </div>
    </div>
</nav>