<div class="container">
  <div class="row">
    <div class="col">
      <p style="color: green"><%= notice %></p>

      <h1>Messages</h1>

      <div id="messages">
        <% @messages.each do |message| %>
          <%= render message %>
        <% end %>
      </div>

      <%= link_to  "Back", portfolio_path(@portfolio) %>    
    </div>
  </div>
</div>

